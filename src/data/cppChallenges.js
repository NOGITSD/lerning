const cppTopics = [
    // 1-20 Beginner
    { t: 'Hello World', d: 'ใช้ cout แสดง "Hello World"', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // เขียนโค้ดที่นี่\n    return 0;\n}\n', h: ['ใช้ cout << "..."', 'อย่าลืม endl หรือ \\n'], hp: 'cout << "Hello World" << endl;', rk: ['cout', '<<'], eo: 'Hello World', ph: true, ml: 4 },
    { t: 'ตัวแปร', d: 'สร้างตัวแปร int, double, string แล้ว cout', s: '#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    // สร้างตัวแปร\n    return 0;\n}\n', h: ['int age = 20;', 'double height = 1.75;', 'string name = "Alex";'], hp: 'int age=20;\ndouble h=1.75;\nstring name="Alex";', rk: ['int', 'double', 'string', 'cout'], ml: 6 },
    { t: 'cin input', d: 'รับ input ชื่อแล้ว cout ทักทาย', s: '#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    // รับ input\n    return 0;\n}\n', h: ['cin >> variable', 'getline(cin, str) สำหรับ string มีช่องว่าง'], hp: 'string name;\ncin >> name;\ncout << "Hello " << name;', rk: ['cin', 'cout'], ml: 6 },
    { t: 'การบวกเลข', d: 'สร้าง a=10, b=20 แล้ว cout ผลบวก', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int a = 10, b = 20;\n    // cout ผลบวก\n    return 0;\n}\n', h: ['cout << a + b'], hp: 'cout << a + b << endl;', rk: ['cout', '+'], eo: '30', ph: true, ml: 5 },
    { t: 'การคำนวณ', d: 'คำนวณพื้นที่วงกลม r=5 (pi*r*r)', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    double r = 5.0;\n    // คำนวณพื้นที่\n    return 0;\n}\n', h: ['M_PI หรือ 3.14159', 'area = pi * r * r'], hp: 'double area = 3.14159 * r * r;', rk: ['double', '*', 'cout'], ml: 5 },
    { t: 'Integer Division', d: 'หาร 17/5 แสดงทั้งผลหาร int และ double', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // integer vs double division\n    return 0;\n}\n', h: ['int/int = int', 'cast เป็น double เพื่อผลทศนิยม'], hp: 'cout << 17/5 << endl;\ncout << 17.0/5.0 << endl;', rk: ['cout', '/'], ml: 5 },
    { t: 'Modulo', d: 'หาเศษ 17%5', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // modulo\n    return 0;\n}\n', h: ['% เหมือน Python'], hp: 'cout << 17%5;', rk: ['%', 'cout'], eo: '2', ph: true, ml: 4 },
    { t: 'const', d: 'สร้าง const PI = 3.14159 แล้วใช้คำนวณ', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // ใช้ const\n    return 0;\n}\n', h: ['const double PI = 3.14159;'], hp: 'const double PI=3.14159;', rk: ['const', 'cout'], ml: 5 },
    { t: 'auto keyword', d: 'ใช้ auto ให้ compiler กำหนด type', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // ใช้ auto\n    return 0;\n}\n', h: ['auto x = 42; // int', 'auto s = "hello"s; // string'], hp: 'auto x=42; auto y=3.14;', rk: ['auto', 'cout'], ml: 5 },
    { t: 'String operations', d: 'รวม string, หาความยาว, เข้าถึงตัวอักษร', s: '#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string a = "Hello";\n    string b = "World";\n    // string operations\n    return 0;\n}\n', h: ['+  รวม string', '.length() หาความยาว', '[i] เข้าถึงตัวอักษร'], hp: 'string c = a + " " + b;\ncout << c.length();', rk: ['string', '+', 'cout'], ml: 6 },
    { t: 'getline', d: 'รับ input ทั้งบรรทัด (มีช่องว่าง)', s: '#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    // getline\n    return 0;\n}\n', h: ['getline(cin, variable)'], hp: 'string line;\ngetline(cin, line);', rk: ['getline', 'cout'], ml: 5 },
    { t: 'Type casting', d: 'แปลง int เป็น double และกลับกัน', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int a = 10;\n    double b = 3.7;\n    // casting\n    return 0;\n}\n', h: ['static_cast<type>(value)', '(type)value แบบ C-style'], hp: 'double c = static_cast<double>(a);', rk: ['static_cast', 'cout'], ml: 6 },
    { t: 'sizeof', d: 'แสดงขนาด memory ของแต่ละ type', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // sizeof\n    return 0;\n}\n', h: ['sizeof(type) return จำนวน bytes'], hp: 'cout << sizeof(int);', rk: ['sizeof', 'cout'], ml: 5 },
    { t: 'Increment/Decrement', d: 'ใช้ ++i, i++, --i, i-- แสดงความแตกต่าง', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int a = 5;\n    // ++, --\n    return 0;\n}\n', h: ['++i เพิ่มก่อนใช้', 'i++ ใช้ก่อนเพิ่ม'], hp: 'cout << ++a; // 6\ncout << a++; // 6 (แล้วเป็น7)', rk: ['++', 'cout'], ml: 5 },
    { t: 'Boolean', d: 'สร้างตัวแปร bool แล้วใช้ logical operators', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    bool a = true, b = false;\n    // logical ops\n    return 0;\n}\n', h: ['&& = and, || = or, ! = not'], hp: 'cout << (a && b); // 0\ncout << (a || b); // 1', rk: ['bool', '&&', '||', 'cout'], ml: 6 },
    { t: 'Swap values', d: 'สลับค่า a กับ b โดยไม่ใช้ swap()', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int a=5, b=10;\n    // swap without swap()\n    return 0;\n}\n', h: ['ใช้ตัวแปร temp', 'temp=a; a=b; b=temp;'], hp: 'int temp=a; a=b; b=temp;', rk: ['int', 'cout'], ml: 6, bp: [{ regex: 'swap\\s*\\(', message: 'ห้ามใช้ swap()! ต้องเขียนเอง' }] },
    { t: 'ternary operator', d: 'ใช้ ternary ตรวจ age>=18', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int age = 20;\n    // ternary\n    return 0;\n}\n', h: ['condition ? val1 : val2'], hp: 'cout << (age>=18 ? "Adult" : "Minor");', rk: ['?', ':', 'cout'], ml: 5 },
    { t: 'enum', d: 'สร้าง enum Color {RED,GREEN,BLUE}', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // enum\n    return 0;\n}\n', h: ['enum Name { VALUE1, VALUE2 };'], hp: 'enum Color {RED,GREEN,BLUE};\nColor c = GREEN;', rk: ['enum', 'cout'], ml: 6 },
    { t: 'typedef / using', d: 'สร้าง type alias ด้วย typedef หรือ using', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    // type alias\n    return 0;\n}\n', h: ['using IntVec = vector<int>;', 'typedef vector<int> IntVec;'], hp: 'using IntVec = vector<int>;\nIntVec v = {1,2,3};', rk: ['using', 'cout'], ml: 6 },
    { t: 'Formatted output', d: 'ใช้ setw, setprecision จัดรูปแบบ output', s: '#include <iostream>\n#include <iomanip>\nusing namespace std;\n\nint main() {\n    double pi = 3.14159265;\n    // formatted output\n    return 0;\n}\n', h: ['setprecision(n)', 'fixed + setprecision'], hp: 'cout << fixed << setprecision(2) << pi;', rk: ['setprecision', 'cout'], ml: 5 },
    // 21-40 Control Flow & Loops
    { t: 'if-else', d: 'ตรวจ score >=50 pass/fail', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int score = 65;\n    // if-else\n    return 0;\n}\n', h: ['if (condition) { } else { }'], hp: 'if(score>=50) cout<<"Pass"; else cout<<"Fail";', rk: ['if', 'else', 'cout'], ml: 6 },
    { t: 'if-else if-else', d: 'ตรวจ grade: A/B/C/F', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int score = 75;\n    // grade\n    return 0;\n}\n', h: ['else if สำหรับหลายเงื่อนไข'], hp: 'if(score>=80) cout<<"A"; else if(score>=60) cout<<"B";', rk: ['if', 'else', 'cout'], ml: 8 },
    { t: 'switch-case', d: 'ใช้ switch แสดงชื่อวัน', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int day = 3;\n    // switch\n    return 0;\n}\n', h: ['switch(var) { case val: ... break; }', 'อย่าลืม break!'], hp: 'switch(day){case 1:cout<<"Mon";break;...}', rk: ['switch', 'case', 'break', 'cout'], ml: 10 },
    { t: 'for loop', d: 'for loop print 1-10', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // for loop\n    return 0;\n}\n', h: ['for (int i=1; i<=10; i++)'], hp: 'for(int i=1;i<=10;i++) cout<<i<<endl;', rk: ['for', 'cout'], ml: 5 },
    { t: 'while loop', d: 'while loop print 1-5', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // while loop\n    return 0;\n}\n', h: ['int i=1; while(i<=5) { ... i++; }'], hp: 'int i=1; while(i<=5){cout<<i<<endl;i++;}', rk: ['while', 'cout'], ml: 6 },
    { t: 'do-while', d: 'do-while loop อย่างน้อย 1 ครั้ง', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // do-while\n    return 0;\n}\n', h: ['do { } while(condition);'], hp: 'int i=1; do{cout<<i;i++;}while(i<=3);', rk: ['do', 'while', 'cout'], ml: 6 },
    { t: 'Nested loops', d: 'print สูตรคูณแม่ 2', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // สูตรคูณ\n    return 0;\n}\n', h: ['for ซ้อน for'], hp: 'for(int j=1;j<=12;j++) cout<<"2x"<<j<<"="<<2*j<<endl;', rk: ['for', 'cout'], ml: 5 },
    { t: 'break & continue', d: 'loop 1-10 skip คู่ หยุดที่ 9', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // break & continue\n    return 0;\n}\n', h: ['continue ข้ามรอบ', 'break หยุด loop'], hp: 'for(int i=1;i<=10;i++){if(i%2==0)continue;if(i>8)break;cout<<i<<endl;}', rk: ['for', 'break', 'continue', 'cout'], ml: 7 },
    { t: 'ผลรวม 1-100', d: 'ใช้ loop หาผลรวม 1-100', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // ผลรวม\n    return 0;\n}\n', h: ['int sum=0; for loop เพิ่มทีละตัว'], hp: 'int sum=0; for(int i=1;i<=100;i++) sum+=i;', rk: ['for', 'cout'], eo: '5050', ph: true, ml: 5 },
    { t: 'FizzBuzz', d: 'FizzBuzz 1-30', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // FizzBuzz\n    return 0;\n}\n', h: ['ตรวจ %15 ก่อน'], hp: 'for(int i=1;i<=30;i++){if(i%15==0)cout<<"FizzBuzz";else if...}', rk: ['for', 'if', 'else', '%', 'cout'], ml: 8 },
    { t: 'Pattern ดาว', d: 'print สามเหลี่ยมดาว 5 แถว', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // star pattern\n    return 0;\n}\n', h: ['แถว i มี i ดาว', 'nested loop หรือ string(i,"*")'], hp: 'for(int i=1;i<=5;i++){for(int j=0;j<i;j++)cout<<"*";cout<<endl;}', rk: ['for', 'cout'], ml: 6 },
    { t: 'เลขจำนวนเฉพาะ', d: 'ตรวจว่า 17 เป็น prime', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int num = 17;\n    // check prime\n    return 0;\n}\n', h: ['หารทดลอง 2 ถึง sqrt(num)', 'ถ้าไม่หารลงตัว = prime'], hp: 'bool prime=true; for(int i=2;i*i<=num;i++) if(num%i==0) prime=false;', rk: ['for', 'if', 'bool', 'cout'], ml: 7 },
    { t: 'Array C-style', d: 'สร้าง array int 5 ตัว แล้ว loop print', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // C-style array\n    return 0;\n}\n', h: ['int arr[5] = {1,2,3,4,5};'], hp: 'int arr[]={1,2,3,4,5}; for(int i=0;i<5;i++) cout<<arr[i];', rk: ['int', '[', ']', 'for', 'cout'], ml: 6 },
    { t: 'vector', d: 'ใช้ vector push_back, size, loop', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    // vector\n    return 0;\n}\n', h: ['vector<int> v;', 'v.push_back(x);'], hp: 'vector<int> v={1,2,3}; v.push_back(4);', rk: ['vector', 'push_back', 'cout'], ml: 7 },
    { t: 'Range-based for', d: 'ใช้ range-based for loop กับ vector', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\nint main() {\n    vector<int> nums = {1,2,3,4,5};\n    // range-based for\n    return 0;\n}\n', h: ['for (auto x : container)', 'for (auto& x : container) สำหรับ reference'], hp: 'for(auto x : nums) cout << x << " ";', rk: ['for', 'auto', ':', 'cout'], ml: 6 },
    { t: '2D Array', d: 'สร้าง matrix 3x3 แล้ว print', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // 2D array\n    return 0;\n}\n', h: ['int m[3][3] = {{1,2,3},{4,5,6},{7,8,9}};'], hp: 'int m[3][3]={{1,2,3},{4,5,6},{7,8,9}};', rk: ['for', 'cout', '['], ml: 8 },
    { t: 'String loop', d: 'นับจำนวนสระใน string', s: '#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string text = "Hello World";\n    // นับสระ\n    return 0;\n}\n', h: ['loop ทุกตัวอักษร', 'ตรวจว่าเป็น a,e,i,o,u'], hp: 'int count=0; for(char c:text) if(string("aeiouAEIOU").find(c)!=string::npos) count++;', rk: ['for', 'if', 'cout'], ml: 7 },
    { t: 'Reverse string', d: 'reverse string โดยไม่ใช้ reverse()', s: '#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string s = "Hello";\n    // reverse\n    return 0;\n}\n', h: ['ใช้ 2 pointers: left, right', 'สลับแล้วเลื่อนเข้าหากัน'], hp: 'int l=0,r=s.length()-1; while(l<r){swap(s[l],s[r]);l++;r--;}', rk: ['while', 'cout'], ml: 7, bp: [{ regex: 'reverse\\s*\\(', message: 'ห้ามใช้ reverse()! ต้องเขียนเอง' }] },
    { t: 'Sorting array', d: 'sort vector ด้วย sort()', s: '#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> v = {5,2,8,1,9};\n    // sort\n    return 0;\n}\n', h: ['sort(v.begin(), v.end())'], hp: 'sort(v.begin(),v.end());', rk: ['sort', 'cout'], ml: 7 },
    // 41-60 Functions & Data Structures
    { t: 'Function พื้นฐาน', d: 'สร้างฟังก์ชัน add(int a, int b)', s: '#include <iostream>\nusing namespace std;\n\n// สร้างฟังก์ชัน add\n\nint main() {\n    cout << add(3, 5) << endl;\n    return 0;\n}\n', h: ['int add(int a, int b) { return a+b; }'], hp: 'int add(int a,int b){return a+b;}', rk: ['int', 'return'], ml: 6 },
    { t: 'Function overloading', d: 'สร้าง add() overload สำหรับ int และ double', s: '#include <iostream>\nusing namespace std;\n\n// overloaded functions\n\nint main() {\n    return 0;\n}\n', h: ['ชื่อเดียวกัน parameter ต่างกัน'], hp: 'int add(int a,int b){return a+b;}\ndouble add(double a,double b){return a+b;}', rk: ['int', 'double', 'return'], ml: 8 },
    { t: 'Pass by reference', d: 'สร้างฟังก์ชัน swap ด้วย reference', s: '#include <iostream>\nusing namespace std;\n\n// swap function\n\nint main() {\n    int a=5, b=10;\n    return 0;\n}\n', h: ['void swap(int& a, int& b)', '& คือ reference'], hp: 'void mySwap(int& a,int& b){int t=a;a=b;b=t;}', rk: ['void', '&'], ml: 7 },
    { t: 'Recursion', d: 'Factorial ด้วย recursion', s: '#include <iostream>\nusing namespace std;\n\n// factorial recursive\n\nint main() {\n    cout << factorial(5) << endl;\n    return 0;\n}\n', h: ['base case: n<=1 return 1', 'recursive: n * factorial(n-1)'], hp: 'int factorial(int n){if(n<=1)return 1;return n*factorial(n-1);}', rk: ['if', 'return'], eo: '120', ph: true, ml: 6 },
    { t: 'Fibonacci', d: 'Fibonacci recursion', s: '#include <iostream>\nusing namespace std;\n\n// fibonacci\n\nint main() {\n    for(int i=0;i<10;i++) cout << fib(i) << " ";\n    return 0;\n}\n', h: ['fib(0)=0, fib(1)=1', 'fib(n) = fib(n-1)+fib(n-2)'], hp: 'int fib(int n){if(n<=1)return n;return fib(n-1)+fib(n-2);}', rk: ['if', 'return'], ml: 6 },
    { t: 'Pointer พื้นฐาน', d: 'สร้าง pointer แล้วเข้าถึงค่าผ่าน dereferencing', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int x = 42;\n    // pointer\n    return 0;\n}\n', h: ['int* ptr = &x;', '*ptr เข้าถึงค่า'], hp: 'int* ptr = &x;\ncout << *ptr;', rk: ['*', '&', 'cout'], ml: 6 },
    { t: 'Pointer arithmetic', d: 'ใช้ pointer เดิน array', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    int arr[] = {10,20,30,40,50};\n    // pointer arithmetic\n    return 0;\n}\n', h: ['int* p = arr;', 'p++ เลื่อนไป element ถัดไป'], hp: 'int* p = arr;\nfor(int i=0;i<5;i++) cout << *(p+i);', rk: ['*', 'cout', 'for'], ml: 7 },
    { t: 'Dynamic memory', d: 'จอง memory ด้วย new แล้วคืนด้วย delete', s: '#include <iostream>\nusing namespace std;\n\nint main() {\n    // dynamic memory\n    return 0;\n}\n', h: ['int* p = new int(42);', 'delete p;'], hp: 'int* p = new int(42);\ncout << *p;\ndelete p;', rk: ['new', 'delete', 'cout'], ml: 6 },
    { t: 'struct', d: 'สร้าง struct Student', s: '#include <iostream>\n#include <string>\nusing namespace std;\n\n// struct\n\nint main() {\n    return 0;\n}\n', h: ['struct Name { type field; };'], hp: 'struct Student{string name;int age;double gpa;};', rk: ['struct', 'cout'], ml: 8 },
    { t: 'class', d: 'สร้าง class Rectangle', s: '#include <iostream>\nusing namespace std;\n\n// class Rectangle\n\nint main() {\n    return 0;\n}\n', h: ['class Name { public: ... private: ... };'], hp: 'class Rectangle{int w,h;public:Rectangle(int w,int h):w(w),h(h){}int area(){return w*h;}};', rk: ['class', 'public', 'return'], ml: 8 },
    { t: 'Constructor/Destructor', d: 'สร้าง class ที่มี constructor, destructor, copy constructor', s: '#include <iostream>\nusing namespace std;\n\n// class with constructors\n\nint main() {\n    return 0;\n}\n', h: ['ClassName() constructor', '~ClassName() destructor'], hp: 'class MyClass{public:MyClass(){cout<<"Created";}~MyClass(){cout<<"Destroyed";}};', rk: ['class', '~', 'cout'], ml: 10 },
    { t: 'Inheritance', d: 'class Animal -> Dog inheritance', s: '#include <iostream>\nusing namespace std;\n\n// inheritance\n\nint main() {\n    return 0;\n}\n', h: ['class Dog : public Animal'], hp: 'class Animal{public:virtual void speak(){cout<<"...";}};class Dog:public Animal{public:void speak()override{cout<<"Woof!";}};', rk: ['class', 'public', 'virtual'], ml: 10 },
    { t: 'Virtual function', d: 'ใช้ virtual function + polymorphism', s: '#include <iostream>\nusing namespace std;\n\n// polymorphism\n\nint main() {\n    return 0;\n}\n', h: ['virtual method ใน base class', 'override ใน derived class'], hp: 'class Shape{public:virtual double area()=0;};', rk: ['virtual', 'class', 'override'], ml: 10 },
    { t: 'Template function', d: 'สร้าง template function สำหรับ max(a,b)', s: '#include <iostream>\nusing namespace std;\n\n// template\n\nint main() {\n    return 0;\n}\n', h: ['template<typename T>', 'T myMax(T a, T b)'], hp: 'template<typename T> T myMax(T a,T b){return a>b?a:b;}', rk: ['template', 'typename', 'return'], ml: 6 },
    { t: 'map (STL)', d: 'ใช้ std::map เก็บ key-value pairs', s: '#include <iostream>\n#include <map>\n#include <string>\nusing namespace std;\n\nint main() {\n    // map\n    return 0;\n}\n', h: ['map<string,int> m;', 'm["key"] = value;'], hp: 'map<string,int> m; m["apple"]=3; m["banana"]=5;', rk: ['map', 'cout', 'for'], ml: 7 },
    { t: 'set (STL)', d: 'ใช้ std::set ลบค่าซ้ำ', s: '#include <iostream>\n#include <set>\nusing namespace std;\n\nint main() {\n    // set\n    return 0;\n}\n', h: ['set<int> s;', 's.insert(x);'], hp: 'set<int> s={3,1,4,1,5,9,2,6}; // auto unique & sorted', rk: ['set', 'cout', 'for'], ml: 7 },
    { t: 'stack (STL)', d: 'ใช้ std::stack', s: '#include <iostream>\n#include <stack>\nusing namespace std;\n\nint main() {\n    // stack\n    return 0;\n}\n', h: ['stack<int> s;', 's.push(x); s.top(); s.pop();'], hp: 'stack<int> s; s.push(1); s.push(2); cout<<s.top();', rk: ['stack', 'push', 'pop', 'cout'], ml: 7 },
    { t: 'queue (STL)', d: 'ใช้ std::queue', s: '#include <iostream>\n#include <queue>\nusing namespace std;\n\nint main() {\n    // queue\n    return 0;\n}\n', h: ['queue<int> q;', 'q.push(x); q.front(); q.pop();'], hp: 'queue<int> q; q.push(1); q.push(2); cout<<q.front();', rk: ['queue', 'push', 'front', 'cout'], ml: 7 },
    { t: 'Algorithm: find/count', d: 'ใช้ algorithm header: find, count', s: '#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> v = {1,2,3,2,1};\n    // find & count\n    return 0;\n}\n', h: ['find(begin,end,val)', 'count(begin,end,val)'], hp: 'auto it=find(v.begin(),v.end(),3);\nint c=count(v.begin(),v.end(),2);', rk: ['find', 'count', 'cout'], ml: 7 },
    { t: 'Lambda (C++)', d: 'ใช้ lambda expression กับ sort', s: '#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    vector<int> v = {5,2,8,1,9};\n    // lambda sort\n    return 0;\n}\n', h: ['[](int a, int b){ return a > b; }'], hp: 'sort(v.begin(),v.end(),[](int a,int b){return a>b;});', rk: ['[', ']', 'sort', 'return'], ml: 7 },
    // 61-80 Advanced
    { t: 'Smart Pointers: unique_ptr', d: 'ใช้ unique_ptr จัดการ memory', s: '#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    // unique_ptr\n    return 0;\n}\n', h: ['auto p = make_unique<int>(42);', 'ไม่ต้อง delete เอง'], hp: 'auto p = make_unique<int>(42);\ncout << *p;', rk: ['unique_ptr', 'cout'], ml: 6 },
    { t: 'Smart Pointers: shared_ptr', d: 'ใช้ shared_ptr', s: '#include <iostream>\n#include <memory>\nusing namespace std;\n\nint main() {\n    // shared_ptr\n    return 0;\n}\n', h: ['auto p = make_shared<int>(42);', 'use_count() ดูจำนวน references'], hp: 'auto p1=make_shared<int>(42); auto p2=p1;\ncout<<p1.use_count();', rk: ['shared_ptr', 'cout'], ml: 6 },
    { t: 'Exception handling', d: 'try-catch จัดการ exception', s: '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\nint main() {\n    // try-catch\n    return 0;\n}\n', h: ['try { throw ... } catch(type& e) { }'], hp: 'try{throw runtime_error("Error!");}catch(exception& e){cout<<e.what();}', rk: ['try', 'catch', 'throw', 'cout'], ml: 7 },
    { t: 'File I/O', d: 'เขียนและอ่านไฟล์ (concept)', s: '#include <iostream>\n#include <fstream>\n#include <string>\nusing namespace std;\n\nint main() {\n    // file I/O concept\n    return 0;\n}\n', h: ['ofstream out("file.txt");', 'ifstream in("file.txt");'], hp: 'ofstream out("test.txt");\nout<<"Hello";\nout.close();', rk: ['ofstream', 'cout'], ml: 7 },
    { t: 'string stream', d: 'ใช้ stringstream แปลง/จัดรูปแบบข้อมูล', s: '#include <iostream>\n#include <sstream>\n#include <string>\nusing namespace std;\n\nint main() {\n    // stringstream\n    return 0;\n}\n', h: ['stringstream ss;', 'ss << value; ss >> variable;'], hp: 'stringstream ss("10 20 30");\nint a,b,c; ss>>a>>b>>c;', rk: ['stringstream', 'cout'], ml: 7 },
    { t: 'Operator Overloading', d: 'overload operator+ สำหรับ Vector2D class', s: '#include <iostream>\nusing namespace std;\n\n// Vector2D\n\nint main() {\n    return 0;\n}\n', h: ['MyClass operator+(const MyClass& other)'], hp: 'class Vec2{public:float x,y;Vec2 operator+(const Vec2& o){return{x+o.x,y+o.y};}};', rk: ['class', 'operator'], ml: 10 },
    { t: 'Template class', d: 'สร้าง template class Stack<T>', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\n// template Stack\n\nint main() {\n    return 0;\n}\n', h: ['template<typename T>', 'class Stack { vector<T> items; };'], hp: 'template<typename T> class Stack{vector<T> items;public:void push(T x){items.push_back(x);}T pop(){T t=items.back();items.pop_back();return t;}};', rk: ['template', 'class', 'typename'], ml: 10 },
    { t: 'Iterator pattern', d: 'สร้าง custom iterator', s: '#include <iostream>\nusing namespace std;\n\n// custom iterator\n\nint main() {\n    return 0;\n}\n', h: ['ต้อง define operator++, operator*, operator!='], hp: 'class Range{int start,stop;...};', rk: ['class', 'operator'], ml: 12 },
    { t: 'Move semantics', d: 'ทำความเข้าใจ move ด้วย std::move', s: '#include <iostream>\n#include <string>\n#include <utility>\nusing namespace std;\n\nint main() {\n    string a = "Hello";\n    // move\n    return 0;\n}\n', h: ['std::move() ย้ายค่า', 'หลัง move ตัวเดิมจะว่าง'], hp: 'string b = std::move(a);\ncout << b;', rk: ['move', 'cout'], ml: 6 },
    { t: 'Linked List implement', d: 'สร้าง Linked List class', s: '#include <iostream>\nusing namespace std;\n\n// LinkedList\n\nint main() {\n    return 0;\n}\n', h: ['struct Node { int val; Node* next; };'], hp: 'struct Node{int val;Node*next;Node(int v):val(v),next(nullptr){}};', rk: ['struct', 'nullptr', 'cout'], ml: 12 },
    { t: 'Binary Search', d: 'เขียน binary search', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\n// binary search\n\nint main() {\n    return 0;\n}\n', h: ['left, right, mid'], hp: 'int bs(vector<int>&a,int t){int l=0,r=a.size()-1;while(l<=r){int m=(l+r)/2;if(a[m]==t)return m;else if(a[m]<t)l=m+1;else r=m-1;}return-1;}', rk: ['while', 'if', 'return'], ml: 8 },
    { t: 'Hash Map implement', d: 'สร้าง simple hash map', s: '#include <iostream>\n#include <vector>\n#include <list>\nusing namespace std;\n\n// HashMap\n\nint main() {\n    return 0;\n}\n', h: ['ใช้ bucket (vector of lists)', 'hash function: key % size'], hp: 'class HashMap{vector<list<pair<int,int>>> buckets;...};', rk: ['class', 'vector', 'list'], ml: 15 },
    { t: 'BFS', d: 'BFS สำหรับ graph', s: '#include <iostream>\n#include <vector>\n#include <queue>\nusing namespace std;\n\n// BFS\n\nint main() {\n    return 0;\n}\n', h: ['ใช้ queue', 'เก็บ visited vector'], hp: 'void bfs(vector<vector<int>>&g,int s){queue<int>q;vector<bool>vis(g.size());q.push(s);vis[s]=true;...}', rk: ['queue', 'while', 'vector', 'void'], ml: 10 },
    { t: 'DFS', d: 'DFS สำหรับ graph (recursive)', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\n// DFS\n\nint main() {\n    return 0;\n}\n', h: ['recursive: เรียกตัวเอง', 'เก็บ visited'], hp: 'void dfs(vector<vector<int>>&g,int n,vector<bool>&vis){vis[n]=true;for(int x:g[n])if(!vis[x])dfs(g,x,vis);}', rk: ['void', 'vector', 'if', 'for'], ml: 8 },
    { t: 'Merge Sort', d: 'implement Merge Sort', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\n// merge sort\n\nint main() {\n    return 0;\n}\n', h: ['แบ่งครึ่ง -> sort -> merge'], hp: 'void mergeSort(vector<int>&a,int l,int r){if(l>=r)return;int m=(l+r)/2;mergeSort(a,l,m);mergeSort(a,m+1,r);...}', rk: ['void', 'vector', 'if'], ml: 15 },
    { t: 'Quick Sort', d: 'implement Quick Sort', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\n// quick sort\n\nint main() {\n    return 0;\n}\n', h: ['partition + recursion', 'pivot เลือกตัวสุดท้าย'], hp: 'int partition(vector<int>&a,int lo,int hi){...}\nvoid qs(vector<int>&a,int lo,int hi){if(lo<hi){int p=partition(a,lo,hi);...}}', rk: ['void', 'vector', 'if', 'swap'], ml: 15 },
    { t: 'Priority Queue', d: 'ใช้ priority_queue', s: '#include <iostream>\n#include <queue>\nusing namespace std;\n\nint main() {\n    // priority queue\n    return 0;\n}\n', h: ['priority_queue<int> pq; (max heap)', 'greater<int> สำหรับ min heap'], hp: 'priority_queue<int> pq; pq.push(3); pq.push(1); pq.push(4);', rk: ['priority_queue', 'push', 'cout'], ml: 7 },
    { t: 'DP: Climbing Stairs', d: 'Climbing Stairs DP', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\n// climbing stairs\n\nint main() {\n    return 0;\n}\n', h: ['dp[i] = dp[i-1] + dp[i-2]'], hp: 'int climb(int n){if(n<=2)return n;vector<int>dp(n+1);dp[1]=1;dp[2]=2;for(int i=3;i<=n;i++)dp[i]=dp[i-1]+dp[i-2];return dp[n];}', rk: ['vector', 'for', 'return'], ml: 8 },
    { t: 'DP: Knapsack', d: '0/1 Knapsack DP', s: '#include <iostream>\n#include <vector>\nusing namespace std;\n\n// knapsack\n\nint main() {\n    return 0;\n}\n', h: ['2D DP table', 'dp[i][w] = max value'], hp: 'int knapsack(vector<int>&wt,vector<int>&val,int cap){...}', rk: ['vector', 'for', 'max', 'return'], ml: 12 },
    { t: 'Final Boss: Expression Parser', d: 'สร้าง expression parser คำนวณ "2+3*4" = 14', s: '#include <iostream>\n#include <string>\n#include <stack>\nusing namespace std;\n\n// expression parser\n\nint main() {\n    return 0;\n}\n', h: ['tokenize', 'ใช้ 2 stacks: numbers + operators', 'operator precedence'], hp: 'int calc(string expr){...}', rk: ['stack', 'while', 'if', 'return'], ml: 20 },
];

function buildCppChallenges() {
    return cppTopics.map((c, i) => ({
        id: i + 1,
        title: c.t,
        description: c.d,
        starterCode: c.s,
        hints: c.h,
        helper: c.hp,
        requiredKeywords: c.rk || [],
        bannedPatterns: c.bp || [],
        requiredPatterns: c.rp || [],
        expectedOutput: c.eo || null,
        preventHardcode: c.ph || false,
        minLines: c.ml || 1,
    }));
}

export const cppChallenges = buildCppChallenges();
