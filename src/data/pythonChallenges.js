const pyTopics = [
    // 1-20 Beginner
    { t: 'Hello World', d: 'ใช้ print() แสดงข้อความ "Hello World"', s: '# เขียนโค้ดที่นี่\n', h: ['ใช้ฟังก์ชัน print()', 'ใส่ข้อความในเครื่องหมายคำพูด'], hp: 'print("Hello World")', rk: ['print'], eo: 'Hello World', ph: false, ml: 1, rp: [{ regex: 'print\\s*\\(', message: 'ต้องใช้ฟังก์ชัน print()' }] },
    { t: 'ตัวแปรแรก', d: 'สร้างตัวแปร name เก็บชื่อของคุณ แล้ว print ออกมา', s: '# สร้างตัวแปร name\n', h: ['สร้างตัวแปรด้วย name = "ชื่อ"', 'ใช้ print(name)'], hp: 'name = "Alex"\nprint(name)', rk: ['print', 'name'], ml: 2, rp: [{ regex: 'name\\s*=\\s*\\(?\\s*["\']', message: 'ต้องสร้างตัวแปร name เก็บ string' }, { regex: 'print\\s*\\(\\s*name\\s*\\)', message: 'ต้องใช้ print(name) เพื่อแสดงค่าตัวแปร' }] },
    { t: 'ชนิดข้อมูล', d: 'สร้างตัวแปร 3 ตัว: age (int), height (float), is_student (bool) แล้ว print ทั้งหมด', s: '# สร้างตัวแปร 3 ชนิด\n', h: ['int เช่น age = 20', 'float เช่น height = 1.75', 'bool เช่น is_student = True'], hp: 'age = 20\nheight = 1.75\nis_student = True\nprint(age)\nprint(height)\nprint(is_student)', rk: ['print'], ml: 4, rp: [{ regex: 'age\\s*=\\s*\\d+', message: 'ต้องสร้างตัวแปร age เป็น int' }, { regex: 'height\\s*=\\s*\\d+\\.\\d+', message: 'ต้องสร้างตัวแปร height เป็น float' }, { regex: 'is_student\\s*=\\s*(True|False)', message: 'ต้องสร้างตัวแปร is_student เป็น bool' }] },
    { t: 'การบวกเลข', d: 'สร้างตัวแปร a = 10, b = 20 แล้ว print ผลบวก', s: 'a = 10\nb = 20\n# print ผลบวก\n', h: ['ใช้เครื่องหมาย + ในการบวก', 'print(a + b)'], hp: 'a = 10\nb = 20\nprint(a + b)', rk: ['print', '+'], eo: '30', ph: true, ml: 3 },
    { t: 'การลบเลข', d: 'สร้างตัวแปร x = 50, y = 18 แล้ว print ผลลบ', s: 'x = 50\ny = 18\n# print ผลลบ\n', h: ['ใช้เครื่องหมาย -'], hp: 'x = 50\ny = 18\nprint(x - y)', rk: ['print', '-'], eo: '32', ph: true, ml: 3 },
    { t: 'การคูณ', d: 'คำนวณพื้นที่สี่เหลี่ยม width=5, height=8 แล้ว print', s: 'width = 5\nheight = 8\n# คำนวณและ print พื้นที่\n', h: ['พื้นที่ = กว้าง x สูง', 'ใช้ * สำหรับคูณ'], hp: 'width = 5\nheight = 8\narea = width * height\nprint(area)', rk: ['*', 'print'], eo: '40', ph: true, ml: 3 },
    { t: 'การหาร', d: 'หาร 100 ด้วย 3 แล้ว print ทั้งผลหารปกติและหารเอาจำนวนเต็ม', s: '# ผลหารปกติ /\n# ผลหารจำนวนเต็ม //\n', h: ['/ คือหารปกติ', '// คือหารเอาจำนวนเต็ม'], hp: 'print(100 / 3)\nprint(100 // 3)', rk: ['/', '//', 'print'], ml: 2, rp: [{ regex: 'print\\s*\\(.*\\/[^/].*\\)', message: 'ต้อง print ผลหารปกติ (/)' }, { regex: 'print\\s*\\(.*\\/\\/.*\\)', message: 'ต้อง print ผลหารจำนวนเต็ม (//)' }] },
    { t: 'Modulo', d: 'หาเศษจากการหาร 17 ด้วย 5 แล้ว print', s: '# ใช้ % หาเศษ\n', h: ['% คือ modulo operator', '17 % 5 = 2'], hp: 'print(17 % 5)', rk: ['%', 'print'], eo: '2', ph: false, ml: 1 },
    { t: 'String Concatenation', d: 'สร้าง first_name และ last_name แล้วรวมกันด้วย + แล้ว print', s: '# สร้างตัวแปรชื่อ\n', h: ['ใช้ + รวม string', 'อย่าลืมเว้นวรรคระหว่างชื่อ'], hp: 'first_name = "John"\nlast_name = "Doe"\nprint(first_name + " " + last_name)', rk: ['+', 'print'], ml: 3, rp: [{ regex: 'first_name\\s*=\\s*\\(?\\s*["\']', message: 'ต้องสร้างตัวแปร first_name' }, { regex: 'last_name\\s*=\\s*\\(?\\s*["\']', message: 'ต้องสร้างตัวแปร last_name' }, { regex: 'first_name\\s*\\+.*last_name|last_name\\s*\\+.*first_name', message: 'ต้องใช้ + รวม string ตัวแปรชื่อ' }] },
    { t: 'f-string', d: 'ใช้ f-string แสดงข้อมูล: "My name is {name} and I am {age} years old"', s: 'name = "Alex"\nage = 20\n# ใช้ f-string\n', h: ['f-string ใช้ f"..." ', 'ใส่ตัวแปรใน {}'], hp: 'name = "Alex"\nage = 20\nprint(f"My name is {name} and I am {age} years old")', rk: ['print', 'f"'], eo: 'My name is Alex and I am 20 years old', ph: false, ml: 3, rp: [{ regex: 'f["\'].*\\{name\\}.*\\{age\\}.*["\']', message: 'ต้องใช้ f-string กับตัวแปร {name} และ {age}' }] },
    { t: 'Input', d: 'รับ input ชื่อจาก user แล้ว print ทักทาย "Hello, {name}!"', s: '# รับ input\n', h: ['ใช้ input() รับข้อมูล', 'เก็บค่าในตัวแปร'], hp: 'name = input("Enter name: ")\nprint(f"Hello, {name}!")', rk: ['input', 'print'], ml: 2, rp: [{ regex: '\\w+\\s*=\\s*input\\s*\\(', message: 'ต้องเก็บค่า input ในตัวแปร' }, { regex: 'print\\s*\\(.*\\w+.*\\)', message: 'ต้อง print ข้อความทักทายที่ใช้ตัวแปร' }] },
    { t: 'แปลงชนิดข้อมูล', d: 'รับ input เป็นตัวเลข แล้วคูณ 2 แล้ว print', s: '# รับตัวเลขจาก input\n', h: ['input() return string เสมอ', 'ใช้ int() แปลงเป็นตัวเลข'], hp: 'num = int(input("Enter number: "))\nprint(num * 2)', rk: ['int(', 'input', 'print'], ml: 2, rp: [{ regex: 'int\\s*\\(\\s*input\\s*\\(', message: 'ต้องใช้ int(input()) เพื่อแปลง input เป็นตัวเลข' }, { regex: '\\*\\s*2', message: 'ต้องคูณ 2' }] },
    { t: 'len()', d: 'สร้าง string แล้วหาความยาวด้วย len() แล้ว print', s: 'text = "Hello Python"\n# หาความยาว\n', h: ['len() หาความยาว'], hp: 'text = "Hello Python"\nprint(len(text))', rk: ['len', 'print'], eo: '12', ph: false, ml: 2 },
    { t: 'String Indexing', d: 'ดึงตัวอักษรตัวแรกและตัวสุดท้ายจาก "Python" ด้วย word[0] และ word[-1] แล้ว print', s: 'word = "Python"\n# ตัวแรก [0] ตัวสุดท้าย [-1]\n', h: ['index เริ่มที่ 0', '[-1] คือตัวสุดท้าย'], hp: 'word = "Python"\nprint(word[0])\nprint(word[-1])', rk: ['[', 'print'], eo: 'P\nn', ph: false, ml: 3, rp: [{ regex: 'word\\s*\\[\\s*0\\s*\\]', message: 'ต้องใช้ word[0] ดึงตัวอักษรตัวแรก' }, { regex: 'word\\s*\\[\\s*-1\\s*\\]', message: 'ต้องใช้ word[-1] ดึงตัวสุดท้าย' }] },
    { t: 'String Slicing', d: 'ตัด string "Hello World" เอาเฉพาะ "World" แล้ว print', s: 'text = "Hello World"\n# ใช้ slicing\n', h: ['ใช้ [start:end]', 'World เริ่มที่ index 6'], hp: 'text = "Hello World"\nprint(text[6:])', rk: ['[', ':', 'print'], eo: 'World', ph: false, ml: 2, rp: [{ regex: 'text\\s*\\[.*:.*\\]', message: 'ต้องใช้ slicing text[start:end]' }] },
    { t: 'upper() lower()', d: 'แปลง "Hello World" เป็นตัวพิมพ์ใหญ่ทั้งหมด แล้ว print อีกบรรทัดแปลงเป็นตัวพิมพ์เล็ก แล้ว print', s: 'text = "Hello World"\n# แปลง\n', h: ['.upper() ทำตัวใหญ่', '.lower() ทำตัวเล็ก'], hp: 'text = "Hello World"\nprint(text.upper())\nprint(text.lower())', rk: ['.upper', '.lower', 'print'], eo: 'HELLO WORLD\nhello world', ph: false, ml: 3, rp: [{ regex: '\\.upper\\s*\\(\\s*\\)', message: 'ต้องใช้ .upper()' }, { regex: '\\.lower\\s*\\(\\s*\\)', message: 'ต้องใช้ .lower()' }] },
    { t: 'replace()', d: 'แทนที่คำว่า "World" ด้วย "Python" ใน "Hello World"', s: 'text = "Hello World"\n# replace\n', h: ['.replace(old, new)'], hp: 'text = "Hello World"\nprint(text.replace("World", "Python"))', rk: ['.replace', 'print'], eo: 'Hello Python', ph: false, ml: 2 },
    { t: 'split()', d: 'แยกประโยค "I love Python programming" เป็นคำๆ แล้ว print', s: 'text = "I love Python programming"\n# split\n', h: ['.split() แยกด้วยช่องว่าง'], hp: 'text = "I love Python programming"\nwords = text.split()\nprint(words)', rk: ['.split', 'print'], ml: 2, rp: [{ regex: '\\.split\\s*\\(', message: 'ต้องใช้ .split()' }] },
    { t: 'Multiple Assignment', d: 'กำหนดค่า x=1, y=2, z=3 ในบรรทัดเดียว แล้ว print ทั้งหมด', s: '# multiple assignment\n', h: ['x, y, z = 1, 2, 3'], hp: 'x, y, z = 1, 2, 3\nprint(x, y, z)', rk: ['print'], ml: 2, rp: [{ regex: '\\w+\\s*,\\s*\\w+\\s*,\\s*\\w+\\s*=', message: 'ต้องใช้ multiple assignment ในบรรทัดเดียว' }] },
    { t: 'Comments', d: 'เขียนโค้ดที่มี comment อธิบาย แล้ว print "Learning Python"', s: '# เขียนโค้ดพร้อม comment\n', h: ['# คือ single line comment'], hp: '# This prints a message\nprint("Learning Python")', rk: ['#', 'print'], eo: 'Learning Python', ph: false, ml: 2 },
    // 21-40 Conditionals &amp; Loops
    { t: 'if statement', d: 'ตรวจสอบว่า age >= 18 หรือไม่ ถ้าใช่ print "Adult" ไม่ใช่ print "Minor"', s: 'age = 20\n# ใช้ if-else\n', h: ['if condition:', 'else:'], hp: 'age = 20\nif age >= 18:\n    print("Adult")\nelse:\n    print("Minor")', rk: ['if', 'print'], ml: 3 },
    { t: 'if-elif-else', d: 'ตรวจ score: >=80 "A", >=60 "B", >=40 "C", น้อยกว่า "F"', s: 'score = 75\n# ใช้ if-elif-else\n', h: ['elif = else if', 'ตรวจจากมากไปน้อย'], hp: 'score = 75\nif score >= 80:\n    print("A")\nelif score >= 60:\n    print("B")\nelif score >= 40:\n    print("C")\nelse:\n    print("F")', rk: ['if', 'elif', 'else', 'print'], ml: 5 },
    { t: 'and / or', d: 'ตรวจสอบว่า age อยู่ระหว่าง 18-60 ใช้ and', s: 'age = 25\n# ใช้ and\n', h: ['and = ทั้งสองเงื่อนไขต้องจริง'], hp: 'age = 25\nif age >= 18 and age <= 60:\n    print("Working age")\nelse:\n    print("Not working age")', rk: ['and', 'if', 'print'], ml: 3 },
    { t: 'not', d: 'ตรวจสอบว่า is_raining ไม่เป็น True ใช้ not', s: 'is_raining = False\n# ใช้ not\n', h: ['not กลับค่า boolean'], hp: 'is_raining = False\nif not is_raining:\n    print("Go outside!")\nelse:\n    print("Stay home")', rk: ['not', 'if', 'print'], ml: 3 },
    { t: 'for loop พื้นฐาน', d: 'ใช้ for loop print ตัวเลข 1-5', s: '# ใช้ for loop\n', h: ['for i in range(1, 6):', 'range(start, stop)'], hp: 'for i in range(1, 6):\n    print(i)', rk: ['for', 'range', 'print'], eo: '1\n2\n3\n4\n5', ph: true, ml: 2, bp: [{ regex: 'print\\s*\\(\\s*["\']1\\\\n2', message: 'ห้าม hard-code! ใช้ for loop' }] },
    { t: 'for loop กับ string', d: 'ใช้ for loop print แต่ละตัวอักษรของ "Python"', s: 'word = "Python"\n# loop แต่ละตัวอักษร\n', h: ['for char in word:'], hp: 'word = "Python"\nfor char in word:\n    print(char)', rk: ['for', 'print'], ml: 2 },
    { t: 'while loop', d: 'ใช้ while loop print เลข 1-5', s: '# ใช้ while loop\n', h: ['กำหนดตัวนับ i = 1', 'while i <= 5:', 'อย่าลืม i += 1'], hp: 'i = 1\nwhile i <= 5:\n    print(i)\n    i += 1', rk: ['while', 'print'], eo: '1\n2\n3\n4\n5', ph: true, ml: 4 },
    { t: 'break', d: 'ใช้ for loop 1-10 แต่ break เมื่อเจอเลข 5', s: '# loop แล้ว break\n', h: ['break หยุด loop ทันที'], hp: 'for i in range(1, 11):\n    if i == 5:\n        break\n    print(i)', rk: ['for', 'break', 'print'], ml: 3 },
    { t: 'continue', d: 'ใช้ for loop 1-10 แต่ skip เลขคู่ ด้วย continue', s: '# skip เลขคู่\n', h: ['continue ข้ามรอบนั้น', 'เลขคู่: i % 2 == 0'], hp: 'for i in range(1, 11):\n    if i % 2 == 0:\n        continue\n    print(i)', rk: ['for', 'continue', 'print'], ml: 3 },
    { t: 'Nested loop', d: 'ใช้ nested loop print สูตรคูณแม่ 2 (2x1=2 ถึง 2x12=24)', s: '# สูตรคูณแม่ 2\n', h: ['loop ใน loop', 'ใช้ for j in range(1,13)'], hp: 'for j in range(1, 13):\n    print(f"2x{j}={2*j}")', rk: ['for', 'print'], ml: 2 },
    { t: 'ผลรวม 1-100', d: 'ใช้ loop คำนวณผลรวม 1 ถึง 100 แล้ว print', s: '# หาผลรวม 1-100\n', h: ['สร้างตัวแปร total = 0', 'เพิ่มค่าทุกรอบ total += i'], hp: 'total = 0\nfor i in range(1, 101):\n    total += i\nprint(total)', rk: ['for', 'print'], eo: '5050', ph: true, ml: 3 },
    { t: 'เลขคู่ เลขคี่', d: 'ใช้ loop แยกเลข 1-20 เป็นคู่และคี่ print แยกกัน', s: '# แยกเลขคู่-คี่\n', h: ['ใช้ % 2 ตรวจคู่/คี่', '== 0 คือเลขคู่'], hp: 'evens = []\nodds = []\nfor i in range(1, 21):\n    if i % 2 == 0:\n        evens.append(i)\n    else:\n        odds.append(i)\nprint("Even:", evens)\nprint("Odd:", odds)', rk: ['for', 'if', '%', 'print'], ml: 4 },
    { t: 'Factorial', d: 'ใช้ loop คำนวณ 5! (factorial) แล้ว print', s: '# คำนวณ 5!\n', h: ['5! = 5×4×3×2×1 = 120', 'เริ่ม result = 1 แล้วคูณทุกรอบ'], hp: 'result = 1\nfor i in range(1, 6):\n    result *= i\nprint(result)', rk: ['for', 'print'], eo: '120', ph: true, ml: 3 },
    { t: 'FizzBuzz', d: 'print 1-30: ถ้าหาร3ลงตัว="Fizz", หาร5="Buzz", ทั้งคู่="FizzBuzz"', s: '# FizzBuzz\n', h: ['ตรวจหาร 15 ก่อน (ทั้ง 3 และ 5)', 'ใช้ elif ต่อ'], hp: 'for i in range(1, 31):\n    if i % 15 == 0:\n        print("FizzBuzz")\n    elif i % 3 == 0:\n        print("Fizz")\n    elif i % 5 == 0:\n        print("Buzz")\n    else:\n        print(i)', rk: ['for', 'if', 'elif', '%', 'print'], ml: 6 },
    { t: 'Pattern: ดาว', d: 'ใช้ nested loop print สามเหลี่ยมดาว 5 แถว: *,**,***,****,*****', s: '# print สามเหลี่ยมดาว\n', h: ['แถวที่ i มี i ดาว', 'ใช้ * กับจำนวนรอบ หรือ nested loop'], hp: 'for i in range(1, 6):\n    print("*" * i)', rk: ['for', 'print'], ml: 2 },
    { t: 'Reverse loop', d: 'ใช้ loop print ตัวเลข 10 ลงมา 1', s: '# นับถอยหลัง 10-1\n', h: ['range(10, 0, -1)', 'step = -1'], hp: 'for i in range(10, 0, -1):\n    print(i)', rk: ['for', 'range', 'print'], ml: 2 },
    { t: 'เลขจำนวนเฉพาะ', d: 'ตรวจสอบว่าเลข 17 เป็นจำนวนเฉพาะหรือไม่', s: 'num = 17\n# ตรวจจำนวนเฉพาะ\n', h: ['หารด้วยเลข 2 ถึง num-1', 'ถ้าไม่มีตัวหารลงตัว = prime'], hp: 'num = 17\nis_prime = True\nfor i in range(2, num):\n    if num % i == 0:\n        is_prime = False\n        break\nif is_prime:\n    print(f"{num} is prime")\nelse:\n    print(f"{num} is not prime")', rk: ['for', 'if', '%', 'print'], ml: 5 },
    { t: 'Guessing Game', d: 'สร้างเกมทาย secret=7 ใช้ while loop ถามจนกว่าจะถูก', s: 'secret = 7\n# สร้างเกมทายเลข\n', h: ['ใช้ while True:', 'ใช้ break เมื่อทายถูก'], hp: 'secret = 7\nwhile True:\n    guess = int(input("Guess: "))\n    if guess == secret:\n        print("Correct!")\n        break\n    elif guess < secret:\n        print("Too low")\n    else:\n        print("Too high")', rk: ['while', 'if', 'input', 'break'], ml: 5 },
    { t: 'List Comprehension เบื้องต้น', d: 'สร้าง list เลขยกกำลัง 2 ของ 1-10 ด้วย list comprehension', s: '# สร้าง list ด้วย comprehension\n', h: ['[expression for item in iterable]', 'i**2 คือยกกำลัง 2'], hp: 'squares = [i**2 for i in range(1, 11)]\nprint(squares)', rk: ['for', '[', 'print'], ml: 2, rp: [{ regex: '\\[.*for.*in.*\\]', message: 'ต้องใช้ list comprehension' }] },
    // 41-60 Functions & Collections
    { t: 'def function', d: 'สร้างฟังก์ชัน greet(name) ที่ return "Hello, {name}!"', s: '# สร้างฟังก์ชัน greet\n', h: ['def greet(name):', 'ใช้ return'], hp: 'def greet(name):\n    return f"Hello, {name}!"\nprint(greet("Alice"))', rk: ['def', 'return', 'print'], ml: 3 },
    { t: 'function with parameters', d: 'สร้างฟังก์ชัน add(a, b) ที่ return ผลบวก', s: '# สร้างฟังก์ชัน add\n', h: ['def add(a, b):', 'return a + b'], hp: 'def add(a, b):\n    return a + b\nprint(add(3, 5))', rk: ['def', 'return', 'print'], ml: 3 },
    { t: 'Default parameter', d: 'สร้างฟังก์ชัน power(base, exp=2) ยกกำลัง', s: '# ฟังก์ชัน power\n', h: ['default parameter ใส่ค่าเริ่มต้น', 'exp=2 คือค่า default'], hp: 'def power(base, exp=2):\n    return base ** exp\nprint(power(3))\nprint(power(2, 10))', rk: ['def', '=', 'return'], ml: 3 },
    { t: '*args', d: 'สร้างฟังก์ชัน sum_all(*args) ที่รวมค่าทั้งหมด', s: '# ฟังก์ชัน sum_all\n', h: ['*args รับหลาย argument', 'ใช้ loop รวมค่า'], hp: 'def sum_all(*args):\n    return sum(args)\nprint(sum_all(1, 2, 3, 4, 5))', rk: ['def', '*args', 'return'], ml: 3 },
    { t: 'Lambda', d: 'สร้าง lambda function สำหรับคูณ 2', s: '# สร้าง lambda\n', h: ['lambda x: expression'], hp: 'double = lambda x: x * 2\nprint(double(5))\nprint(double(10))', rk: ['lambda', 'print'], ml: 2 },
    { t: 'List พื้นฐาน', d: 'สร้าง list ผลไม้ 5 ชนิด แล้ว print ทั้ง list และ item ที่ 3', s: '# สร้าง list\n', h: ['fruits = ["apple", ...]', 'index เริ่มที่ 0'], hp: 'fruits = ["apple", "banana", "cherry", "date", "elderberry"]\nprint(fruits)\nprint(fruits[2])', rk: ['[', ']', 'print'], ml: 3 },
    { t: 'List methods', d: 'ใช้ append, insert, remove กับ list', s: 'fruits = ["apple", "banana"]\n# ใช้ methods\n', h: ['.append() เพิ่มท้าย', '.insert(i, x) แทรก', '.remove(x) ลบ'], hp: 'fruits = ["apple", "banana"]\nfruits.append("cherry")\nfruits.insert(0, "avocado")\nfruits.remove("banana")\nprint(fruits)', rk: ['.append', '.insert', '.remove', 'print'], ml: 5 },
    { t: 'List sorting', d: 'sort list ตัวเลข [5,2,8,1,9,3] จากน้อยไปมากและมากไปน้อย', s: 'nums = [5, 2, 8, 1, 9, 3]\n# sort\n', h: ['.sort() ascending', 'sorted(..., reverse=True)'], hp: 'nums = [5, 2, 8, 1, 9, 3]\nnums.sort()\nprint("Ascending:", nums)\nprint("Descending:", sorted(nums, reverse=True))', rk: ['.sort', 'print'], ml: 3 },
    { t: 'Dictionary', d: 'สร้าง dict เก็บข้อมูลนักเรียน (name, age, grade) แล้ว print', s: '# สร้าง dictionary\n', h: ['student = {"name": "...", ...}', 'เข้าถึงด้วย dict["key"]'], hp: 'student = {"name": "Alex", "age": 18, "grade": "A"}\nprint(student)\nprint(student["name"])', rk: ['{', '}', 'print'], ml: 3 },
    { t: 'Dict methods', d: 'ใช้ .keys(), .values(), .items() กับ dictionary', s: 'student = {"name":"Alex","age":18,"grade":"A"}\n# ใช้ methods\n', h: ['.keys() ได้ key ทั้งหมด', '.values() ได้ value ทั้งหมด'], hp: 'student = {"name": "Alex", "age": 18, "grade": "A"}\nprint(student.keys())\nprint(student.values())\nprint(student.items())', rk: ['.keys', '.values', '.items', 'print'], ml: 4 },
    { t: 'Tuple', d: 'สร้าง tuple เก็บพิกัด (x, y, z) แล้ว unpack', s: '# สร้าง tuple\n', h: ['coords = (1, 2, 3)', 'x, y, z = coords'], hp: 'coords = (10, 20, 30)\nx, y, z = coords\nprint(x, y, z)', rk: ['(', 'print'], ml: 3 },
    { t: 'Set', d: 'สร้าง set 2 ชุด แล้วหา union, intersection', s: '# สร้าง set\n', h: ['set_a = {1, 2, 3}', 'union: | intersection: &'], hp: 'a = {1, 2, 3}\nb = {2, 3, 4}\nprint("Union:", a | b)\nprint("Intersection:", a & b)', rk: ['{', '}', 'print'], ml: 4 },
    { t: 'map()', d: 'ใช้ map() ยกกำลัง 2 ทุกตัวใน list [1,2,3,4,5]', s: 'nums = [1, 2, 3, 4, 5]\n# ใช้ map\n', h: ['map(function, iterable)', 'ใช้กับ lambda ได้'], hp: 'nums = [1, 2, 3, 4, 5]\nresult = list(map(lambda x: x**2, nums))\nprint(result)', rk: ['map', 'print'], ml: 2 },
    { t: 'filter()', d: 'ใช้ filter() กรองเฉพาะเลขคู่จาก [1-10]', s: 'nums = list(range(1, 11))\n# ใช้ filter\n', h: ['filter(function, iterable)', 'เลขคู่: x % 2 == 0'], hp: 'nums = list(range(1, 11))\nevens = list(filter(lambda x: x % 2 == 0, nums))\nprint(evens)', rk: ['filter', 'print'], ml: 2 },
    { t: 'enumerate()', d: 'ใช้ enumerate() loop พร้อม index', s: 'fruits = ["apple","banana","cherry"]\n# ใช้ enumerate\n', h: ['for i, item in enumerate(list):'], hp: 'fruits = ["apple", "banana", "cherry"]\nfor i, f in enumerate(fruits):\n    print(i, f)', rk: ['enumerate', 'for', 'print'], ml: 2 },
    { t: 'zip()', d: 'ใช้ zip() รวม 2 lists เข้าด้วยกัน', s: 'names = ["Alice","Bob"]\nages = [25, 30]\n# ใช้ zip\n', h: ['zip(list1, list2)'], hp: 'names = ["Alice", "Bob"]\nages = [25, 30]\nfor n, a in zip(names, ages):\n    print(f"{n} is {a} years old")', rk: ['zip', 'for', 'print'], ml: 2 },
    { t: 'ฟังก์ชัน Recursive', d: 'สร้างฟังก์ชัน recursive คำนวณ fibonacci(n)', s: '# fibonacci recursive\n', h: ['base case: n<=1 return n', 'recursive: fib(n-1)+fib(n-2)'], hp: 'def fib(n):\n    if n <= 1:\n        return n\n    return fib(n-1) + fib(n-2)\nfor i in range(10):\n    print(fib(i))', rk: ['def', 'return', 'if'], ml: 4 },
    { t: 'String Methods รวม', d: 'นับจำนวนสระใน string ด้วย loop + if', s: 'text = "Hello World Python"\n# นับสระ\n', h: ['สระ: a,e,i,o,u', 'ใช้ in operator'], hp: 'text = "Hello World Python"\ncount = sum(1 for c in text.lower() if c in "aeiou")\nprint(f"Vowels: {count}")', rk: ['for', 'if', 'print'], ml: 3 },
    { t: 'try-except', d: 'เขียนโค้ดที่จัดการ error เมื่อหารด้วย 0', s: '# จัดการ error\n', h: ['try: ... except: ...', 'ZeroDivisionError'], hp: 'try:\n    result = 10 / 0\n    print(result)\nexcept ZeroDivisionError:\n    print("Cannot divide by zero")', rk: ['try', 'except', 'print'], ml: 4 },
    { t: 'File-like operations', d: 'สร้าง list แล้วเขียนข้อมูลลงไป อ่านออกมา (จำลอง file)', s: '# จำลองการทำงานกับข้อมูล\n', h: ['ใช้ list เก็บ lines', 'ใช้ loop อ่าน'], hp: 'lines = []\nlines.append("Hello")\nlines.append("World")\nlines.append("Python")\nfor line in lines:\n    print(line)', rk: ['for', 'print', '.append'], ml: 5 },
    // 61-80 Advanced
    { t: 'Class พื้นฐาน', d: 'สร้าง class Dog ที่มี __init__, bark()', s: '# สร้าง class Dog\n', h: ['class Dog:', 'def __init__(self, name):'], hp: 'class Dog:\n    def __init__(self, name):\n        self.name = name\n    def bark(self):\n        return f"{self.name} says Woof!"\ndog = Dog("Buddy")\nprint(dog.bark())', rk: ['class', 'def', '__init__', 'self'], ml: 5 },
    { t: 'Inheritance', d: 'สร้าง class Animal แล้วให้ Cat สืบทอด', s: '# สร้าง class Animal และ Cat\n', h: ['class Cat(Animal):', 'super().__init__()'], hp: 'class Animal:\n    def __init__(self, name):\n        self.name = name\n    def speak(self):\n        return "..."\nclass Cat(Animal):\n    def speak(self):\n        return f"{self.name} says Meow!"\ncat = Cat("Luna")\nprint(cat.speak())', rk: ['class', 'def', 'super'], ml: 8 },
    { t: 'Magic Methods', d: 'สร้าง class Point ที่มี __str__ และ __add__', s: '# class Point\n', h: ['__str__ ใช้ตอน print', '__add__ ใช้ตอน +'], hp: 'class Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n    def __str__(self):\n        return f"({self.x}, {self.y})"\n    def __add__(self, other):\n        return Point(self.x + other.x, self.y + other.y)\np1 = Point(1, 2)\np2 = Point(3, 4)\nprint(p1 + p2)', rk: ['class', '__str__', '__add__'], ml: 8 },
    { t: 'Property', d: 'ใช้ @property สำหรับ getter/setter ใน class', s: '# ใช้ @property\n', h: ['@property decorator', '@name.setter'], hp: 'class Person:\n    def __init__(self, name):\n        self._name = name\n    @property\n    def name(self):\n        return self._name\n    @name.setter\n    def name(self, value):\n        self._name = value\np = Person("Alice")\nprint(p.name)\np.name = "Bob"\nprint(p.name)', rk: ['@property', 'class', 'def'], ml: 8 },
    { t: 'List flattening', d: 'แปลง nested list [[1,2],[3,4],[5,6]] เป็น [1,2,3,4,5,6]', s: 'nested = [[1,2],[3,4],[5,6]]\n# flatten\n', h: ['ใช้ nested comprehension', '[x for sub in nested for x in sub]'], hp: 'nested = [[1, 2], [3, 4], [5, 6]]\nflat = [x for sub in nested for x in sub]\nprint(flat)', rk: ['for', 'print'], ml: 2 },
    { t: 'Dictionary Comprehension', d: 'สร้าง dict {x: x**2} สำหรับ x 1-10', s: '# dict comprehension\n', h: ['{key: value for item in iterable}'], hp: 'squares = {x: x**2 for x in range(1, 11)}\nprint(squares)', rk: ['{', '}', 'for', 'print'], ml: 2, rp: [{ regex: '\\{.*:.*for.*in.*\\}', message: 'ต้องใช้ dict comprehension' }] },
    { t: 'Generator', d: 'สร้าง generator function ที่ yield เลข fibonacci', s: '# fibonacci generator\n', h: ['ใช้ yield แทน return', 'yield ทำให้เป็น generator'], hp: 'def fib_gen(n):\n    a, b = 0, 1\n    for _ in range(n):\n        yield a\n        a, b = b, a + b\nfor num in fib_gen(10):\n    print(num)', rk: ['def', 'yield'], ml: 5 },
    { t: 'Decorator', d: 'สร้าง decorator ที่ print เวลาทำงานของฟังก์ชัน', s: 'import time\n# สร้าง decorator\n', h: ['def decorator(func):', 'def wrapper(*args):'], hp: 'import time\ndef timer(func):\n    def wrapper(*args):\n        start = time.time()\n        result = func(*args)\n        end = time.time()\n        print(f"{func.__name__} took {end-start:.4f}s")\n        return result\n    return wrapper\n@timer\ndef slow_add(a, b):\n    time.sleep(0.1)\n    return a + b\nprint(slow_add(3, 5))', rk: ['def', 'def', 'return'], ml: 7 },
    { t: 'Context Manager', d: 'สร้าง context manager ง่ายๆ ด้วย class ที่มี __enter__, __exit__', s: '# context manager\n', h: ['__enter__ ทำตอนเข้า', '__exit__ ทำตอนออก'], hp: 'class MyCtx:\n    def __enter__(self):\n        print("Entering context")\n        return self\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        print("Exiting context")\n        return False\nwith MyCtx() as ctx:\n    print("Inside context")', rk: ['class', '__enter__', '__exit__'], ml: 7 },
    { t: 'Regular Expression', d: 'ใช้ re module หา email addresses จาก string', s: 'import re\ntext = "Contact us at info@example.com or support@test.org"\n# หา email\n', h: ['re.findall(pattern, string)', 'email pattern: \\w+@\\w+\\.\\w+'], hp: 'import re\ntext = "Contact us at info@example.com or support@test.org"\nemails = re.findall(r"\w+@\w+\.\w+", text)\nprint(emails)', rk: ['re', 'findall', 'print'], ml: 3 },
    { t: 'Counter', d: 'ใช้ Counter นับความถี่ตัวอักษรใน string', s: 'from collections import Counter\n# นับตัวอักษร\n', h: ['Counter(string)', 'most_common(n)'], hp: 'from collections import Counter\nc = Counter("hello world")\nprint(c)\nprint(c.most_common(3))', rk: ['Counter', 'print'], ml: 3 },
    { t: 'defaultdict', d: 'ใช้ defaultdict จัดกลุ่มคำตามตัวอักษรแรก', s: 'from collections import defaultdict\nwords = ["apple","avocado","banana","cherry","coconut"]\n# จัดกลุ่ม\n', h: ['defaultdict(list)', 'append ตามตัวอักษรแรก'], hp: 'from collections import defaultdict\nwords = ["apple", "avocado", "banana", "cherry", "coconut"]\ngroups = defaultdict(list)\nfor w in words:\n    groups[w[0]].append(w)\nfor key, val in groups.items():\n    print(f"{key}: {val}")', rk: ['defaultdict', 'for', 'print'], ml: 4 },
    { t: 'Sorting with key', d: 'sort list of dicts ตาม age', s: 'people = [{"name":"Bob","age":25},{"name":"Alice","age":20},{"name":"Charlie","age":30}]\n# sort by age\n', h: ['sorted(list, key=...)', 'key=lambda x: x["age"]'], hp: 'people = [{"name":"Bob","age":25},{"name":"Alice","age":20},{"name":"Charlie","age":30}]\nsorted_p = sorted(people, key=lambda x: x["age"])\nfor p in sorted_p:\n    print(f"{p["name"]}: {p["age"]}")', rk: ['sorted', 'lambda', 'key', 'print'], ml: 2 },
    { t: 'Matrix operations', d: 'สร้าง matrix 3x3 แล้วคำนวณ transpose', s: '# matrix transpose\n', h: ['matrix เป็น list of lists', 'transpose: แถว->สดมภ์'], hp: 'm = [[1,2,3],[4,5,6],[7,8,9]]\nt = [[m[j][i] for j in range(3)] for i in range(3)]\nprint("Original:", m)\nprint("Transpose:", t)', rk: ['for', 'print'], ml: 4 },
    { t: 'Binary Search', d: 'เขียนฟังก์ชัน binary_search(arr, target)', s: '# binary search\n', h: ['left, right pointers', 'mid = (left+right)//2'], hp: 'def binary_search(arr, target):\n    left, right = 0, len(arr) - 1\n    while left <= right:\n        mid = (left + right) // 2\n        if arr[mid] == target:\n            return mid\n        elif arr[mid] < target:\n            left = mid + 1\n        else:\n            right = mid - 1\n    return -1\nnums = [1, 3, 5, 7, 9, 11]\nprint(binary_search(nums, 7))', rk: ['def', 'while', 'return'], ml: 8 },
    { t: 'Stack', d: 'สร้าง Stack class ด้วย list (push, pop, peek)', s: '# Stack class\n', h: ['ใช้ list เป็น internal storage', '.append() = push, .pop() = pop'], hp: 'class Stack:\n    def __init__(self):\n        self.items = []\n    def push(self, x):\n        self.items.append(x)\n    def pop(self):\n        return self.items.pop()\n    def peek(self):\n        return self.items[-1]\ns = Stack()\ns.push(1)\ns.push(2)\ns.push(3)\nprint(s.pop())\nprint(s.peek())', rk: ['class', 'def', 'append', 'pop'], ml: 8 },
    { t: 'Queue', d: 'สร้าง Queue class (enqueue, dequeue)', s: '# Queue class\n', h: ['FIFO: First In, First Out', '.pop(0) หรือ collections.deque'], hp: 'from collections import deque\nclass Queue:\n    def __init__(self):\n        self.q = deque()\n    def enqueue(self, x):\n        self.q.append(x)\n    def dequeue(self):\n        return self.q.popleft()\nq = Queue()\nq.enqueue("A")\nq.enqueue("B")\nprint(q.dequeue())', rk: ['class', 'def'], ml: 8 },
    { t: 'Linked List', d: 'สร้าง simple linked list ด้วย Node class', s: '# Linked List\n', h: ['class Node: val, next', 'class LinkedList: head'], hp: 'class Node:\n    def __init__(self, val):\n        self.val = val\n        self.next = None\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    def append(self, val):\n        node = Node(val)\n        if not self.head:\n            self.head = node\n            return\n        cur = self.head\n        while cur.next:\n            cur = cur.next\n        cur.next = node\nll = LinkedList()\nll.append(1)\nll.append(2)\nll.append(3)\ncur = ll.head\nwhile cur:\n    print(cur.val)\n    cur = cur.next', rk: ['class', 'def', 'self', 'None'], ml: 10 },
    { t: 'Memoization', d: 'ใช้ memoization เร่ง fibonacci', s: '# fibonacci with memoization\n', h: ['ใช้ dict เก็บค่าที่คำนวณแล้ว', 'ตรวจก่อนว่าคำนวณยัง'], hp: 'memo = {}\ndef fib(n):\n    if n in memo:\n        return memo[n]\n    if n <= 1:\n        return n\n    memo[n] = fib(n-1) + fib(n-2)\n    return memo[n]\nfor i in range(10):\n    print(fib(i))', rk: ['def', 'if', 'return'], ml: 6 },
    { t: 'Higher-Order Function', d: 'สร้างฟังก์ชันที่ return ฟังก์ชัน (closure)', s: '# closure\n', h: ['ฟังก์ชันซ้อนฟังก์ชัน', 'inner ใช้ตัวแปร outer ได้'], hp: 'def multiplier(n):\n    def inner(x):\n        return x * n\n    return inner\ndouble = multiplier(2)\ntriple = multiplier(3)\nprint(double(5))\nprint(triple(5))', rk: ['def', 'return', 'def'], ml: 5 },
    // 81-100 Algorithms
    { t: 'Bubble Sort', d: 'เขียน Bubble Sort สำหรับ list ตัวเลข', s: '# bubble sort\n', h: ['เปรียบเทียบคู่ที่อยู่ติดกัน', 'สลับถ้าไม่เรียง'], hp: 'def bubble_sort(arr):\n    n = len(arr)\n    for i in range(n):\n        for j in range(n - 1 - i):\n            if arr[j] > arr[j+1]:\n                arr[j], arr[j+1] = arr[j+1], arr[j]\n    return arr\nprint(bubble_sort([5, 2, 8, 1, 9]))', rk: ['def', 'for', 'if'], ml: 6 },
    { t: 'Selection Sort', d: 'เขียน Selection Sort', s: '# selection sort\n', h: ['หาค่าน้อยสุดในส่วนที่ยังไม่ sort', 'สลับไปตำแหน่งที่ถูกต้อง'], hp: 'def selection_sort(arr):\n    for i in range(len(arr)):\n        min_idx = i\n        for j in range(i+1, len(arr)):\n            if arr[j] < arr[min_idx]:\n                min_idx = j\n        arr[i], arr[min_idx] = arr[min_idx], arr[i]\n    return arr\nprint(selection_sort([5, 2, 8, 1, 9]))', rk: ['def', 'for', 'if', 'min'], ml: 6 },
    { t: 'Insertion Sort', d: 'เขียน Insertion Sort', s: '# insertion sort\n', h: ['หยิบ element ทีละตัว', 'แทรกในตำแหน่งที่ถูกต้อง'], hp: 'def insertion_sort(arr):\n    for i in range(1, len(arr)):\n        key = arr[i]\n        j = i - 1\n        while j >= 0 and arr[j] > key:\n            arr[j+1] = arr[j]\n            j -= 1\n        arr[j+1] = key\n    return arr\nprint(insertion_sort([5, 2, 8, 1, 9]))', rk: ['def', 'for', 'while'], ml: 6 },
    { t: 'Merge Sort', d: 'เขียน Merge Sort (recursive)', s: '# merge sort\n', h: ['แบ่งครึ่ง -> sort แต่ละส่วน -> merge', 'base case: len(arr) <= 1'], hp: 'def merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    result = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            result.append(left[i])\n            i += 1\n        else:\n            result.append(right[j])\n            j += 1\n    result.extend(left[i:])\n    result.extend(right[j:])\n    return result\nprint(merge_sort([5, 2, 8, 1, 9]))', rk: ['def', 'return', 'if'], ml: 10 },
    { t: 'Quick Sort', d: 'เขียน Quick Sort (recursive)', s: '# quick sort\n', h: ['เลือก pivot', 'แบ่งเป็น less, equal, greater'], hp: 'def quick_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    pivot = arr[0]\n    less = [x for x in arr[1:] if x <= pivot]\n    greater = [x for x in arr[1:] if x > pivot]\n    return quick_sort(less) + [pivot] + quick_sort(greater)\nprint(quick_sort([5, 2, 8, 1, 9]))', rk: ['def', 'return', 'if'], ml: 8 },
    { t: 'Two Sum', d: 'หา 2 ตัวเลขใน list ที่รวมกันได้ target (ใช้ dict)', s: '# two sum\n', h: ['ใช้ dict เก็บเลขที่เห็นแล้ว', 'complement = target - num'], hp: 'def two_sum(nums, target):\n    seen = {}\n    for i, n in enumerate(nums):\n        complement = target - n\n        if complement in seen:\n            return [seen[complement], i]\n        seen[n] = i\n    return []\nprint(two_sum([2, 7, 11, 15], 9))', rk: ['def', 'for', 'if', 'return'], ml: 6 },
    { t: 'Palindrome Check', d: 'ตรวจสอบว่า string เป็น palindrome หรือไม่', s: '# palindrome check\n', h: ['palindrome อ่านจากหน้า/หลังเหมือนกัน', 'เทียบ string กับ reverse'], hp: 'def is_palindrome(s):\n    s = s.lower()\n    return s == s[::-1]\nprint(is_palindrome("racecar"))\nprint(is_palindrome("hello"))', rk: ['def', 'return'], ml: 3 },
    { t: 'Anagram Check', d: 'ตรวจสอบว่า 2 string เป็น anagram กัน', s: '# anagram check\n', h: ['anagram: ตัวอักษรเหมือนกัน แค่สลับที่', 'sort ทั้ง 2 แล้วเทียบ'], hp: 'def is_anagram(s1, s2):\n    return sorted(s1.lower()) == sorted(s2.lower())\nprint(is_anagram("listen", "silent"))\nprint(is_anagram("hello", "world"))', rk: ['def', 'return', 'sorted'], ml: 3 },
    { t: 'Matrix Spiral', d: 'print matrix 3x3 เป็น spiral order', s: 'matrix = [[1,2,3],[4,5,6],[7,8,9]]\n# spiral order\n', h: ['ไปขวา->ล่าง->ซ้าย->ขึ้น', 'ใช้ boundaries: top,bottom,left,right'], hp: 'def spiral(matrix):\n    result = []\n    while matrix:\n        result += matrix.pop(0)\n        matrix = list(zip(*matrix))[::-1]\n    return result\nm = [[1,2,3],[4,5,6],[7,8,9]]\nprint(spiral(m))', rk: ['def', 'while', 'print'], ml: 10 },
    { t: 'GCD & LCM', d: 'เขียนฟังก์ชัน gcd(a,b) และ lcm(a,b)', s: '# GCD and LCM\n', h: ['GCD: Euclidean algorithm', 'LCM = a*b // gcd(a,b)'], hp: 'def gcd(a, b):\n    while b:\n        a, b = b, a % b\n    return a\ndef lcm(a, b):\n    return a * b // gcd(a, b)\nprint(f"GCD(12,8) = {gcd(12,8)}")\nprint(f"LCM(12,8) = {lcm(12,8)}")', rk: ['def', 'while', 'return'], ml: 5 },
    { t: 'Caesar Cipher', d: 'เขียน encrypt/decrypt ด้วย Caesar Cipher', s: '# Caesar Cipher\n', h: ['เลื่อนตัวอักษร n ตำแหน่ง', 'ใช้ ord() และ chr()'], hp: 'def encrypt(text, shift):\n    result = ""\n    for c in text:\n        if c.isalpha():\n            base = ord("A") if c.isupper() else ord("a")\n            result += chr((ord(c) - base + shift) % 26 + base)\n        else:\n            result += c\n    return result\nprint(encrypt("Hello World", 3))\nprint(encrypt(encrypt("Hello World", 3), -3))', rk: ['def', 'for', 'chr', 'ord'], ml: 6 },
    { t: 'Roman to Integer', d: 'แปลงเลขโรมัน เช่น "XIV" เป็น 14', s: '# Roman to Integer\n', h: ['สร้าง dict mapping: I=1, V=5, ...', 'ถ้าตัวเล็กอยู่ก่อนตัวใหญ่ = ลบ'], hp: 'def roman_to_int(s):\n    vals = {"I":1,"V":5,"X":10,"L":50,"C":100,"D":500,"M":1000}\n    total = 0\n    for i in range(len(s)):\n        if i+1 < len(s) and vals[s[i]] < vals[s[i+1]]:\n            total -= vals[s[i]]\n        else:\n            total += vals[s[i]]\n    return total\nprint(roman_to_int("XIV"))\nprint(roman_to_int("MCMXCIV"))', rk: ['def', 'for', 'if', 'return'], ml: 8 },
    { t: 'Valid Parentheses', d: 'ตรวจสอบว่า string ของวงเล็บ (){}[] ถูกต้อง', s: '# valid parentheses\n', h: ['ใช้ Stack', 'เปิดวงเล็บ push, ปิดวงเล็บ pop&ตรวจ'], hp: 'def is_valid(s):\n    stack = []\n    pairs = {"(": ")", "{": "}", "[": "]"}\n    for c in s:\n        if c in pairs:\n            stack.append(pairs[c])\n        elif not stack or stack.pop() != c:\n            return False\n    return len(stack) == 0\nprint(is_valid("({[]})"))\nprint(is_valid("([)]"))', rk: ['def', 'for', 'if', 'return'], ml: 8 },
    { t: 'Flatten Nested Dict', d: 'แปลง nested dict ให้เป็น flat dict ด้วย dot notation keys', s: '# flatten dict\n', h: ['ใช้ recursion', 'key ซ้อน = "parent.child"'], hp: 'def flatten(d, parent=""):\n    items = {}\n    for k, v in d.items():\n        key = f"{parent}.{k}" if parent else k\n        if isinstance(v, dict):\n            items.update(flatten(v, key))\n        else:\n            items[key] = v\n    return items\nnested = {"a": 1, "b": {"c": 2, "d": {"e": 3}}}\nprint(flatten(nested))', rk: ['def', 'for', 'if', 'return'], ml: 8 },
    { t: 'Matrix Multiplication', d: 'เขียนฟังก์ชันคูณ matrix 2 ตัว', s: '# matrix multiplication\n', h: ['result[i][j] = sum(A[i][k]*B[k][j])', 'ใช้ 3 nested loops'], hp: 'def mat_mul(A, B):\n    rows_A, cols_A = len(A), len(A[0])\n    cols_B = len(B[0])\n    result = [[0]*cols_B for _ in range(rows_A)]\n    for i in range(rows_A):\n        for j in range(cols_B):\n            for k in range(cols_A):\n                result[i][j] += A[i][k] * B[k][j]\n    return result\nA = [[1,2],[3,4]]\nB = [[5,6],[7,8]]\nprint(mat_mul(A, B))', rk: ['def', 'for', 'return'], ml: 8 },
    { t: 'Tree Traversal', d: 'สร้าง Binary Tree แล้วทำ inorder traversal', s: '# binary tree traversal\n', h: ['class TreeNode: val, left, right', 'inorder: left->root->right'], hp: 'class TreeNode:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\ndef inorder(node):\n    if node is None:\n        return []\n    return inorder(node.left) + [node.val] + inorder(node.right)\nroot = TreeNode(4)\nroot.left = TreeNode(2)\nroot.right = TreeNode(6)\nroot.left.left = TreeNode(1)\nroot.left.right = TreeNode(3)\nprint(inorder(root))', rk: ['class', 'def', 'if', 'return'], ml: 10 },
    { t: 'Graph BFS', d: 'เขียน BFS สำหรับ graph (adjacency list)', s: 'from collections import deque\n# BFS\n', h: ['ใช้ queue (deque)', 'เก็บ visited set'], hp: 'from collections import deque\ndef bfs(graph, start):\n    visited = set()\n    queue = deque([start])\n    result = []\n    while queue:\n        node = queue.popleft()\n        if node not in visited:\n            visited.add(node)\n            result.append(node)\n            queue.extend(graph.get(node, []))\n    return result\ngraph = {"A":["B","C"],"B":["D"],"C":["E"],"D":[],"E":[]}\nprint(bfs(graph, "A"))', rk: ['def', 'while', 'deque'], ml: 8 },
    { t: 'Dynamic Programming', d: 'แก้ปัญหา Climbing Stairs (กี่วิธีขึ้นบันได n ขั้น ทีละ 1-2 ขั้น)', s: '# climbing stairs DP\n', h: ['dp[i] = dp[i-1] + dp[i-2]', 'คล้าย fibonacci'], hp: 'def climb(n):\n    if n <= 2:\n        return n\n    dp = [0] * (n + 1)\n    dp[1], dp[2] = 1, 2\n    for i in range(3, n + 1):\n        dp[i] = dp[i-1] + dp[i-2]\n    return dp[n]\nfor i in range(1, 8):\n    print(f"stairs={i}: {climb(i)} ways")', rk: ['def', 'for', 'return'], ml: 6 },
    { t: 'LRU Cache', d: 'สร้าง LRU Cache ด้วย OrderedDict', s: 'from collections import OrderedDict\n# LRU Cache\n', h: ['OrderedDict เก็บลำดับ', 'move_to_end เมื่อ access'], hp: 'from collections import OrderedDict\nclass LRUCache:\n    def __init__(self, capacity):\n        self.cache = OrderedDict()\n        self.cap = capacity\n    def get(self, key):\n        if key not in self.cache:\n            return -1\n        self.cache.move_to_end(key)\n        return self.cache[key]\n    def put(self, key, value):\n        if key in self.cache:\n            self.cache.move_to_end(key)\n        self.cache[key] = value\n        if len(self.cache) > self.cap:\n            self.cache.popitem(last=False)\ncache = LRUCache(2)\ncache.put(1, "a")\ncache.put(2, "b")\nprint(cache.get(1))\ncache.put(3, "c")\nprint(cache.get(2))', rk: ['class', 'def', 'OrderedDict'], ml: 12 },
    { t: 'Final Boss: Mini Interpreter', d: 'สร้าง mini calculator interpreter: รับ string "2 + 3 * 4" แล้วคำนวณ (เรียงตามลำดับ operator precedence)', s: '# mini interpreter\n', h: ['tokenize string', 'ทำ * / ก่อน + -', 'ใช้ stack'], hp: 'def calc(expr):\n    tokens = expr.split()\n    nums = [float(tokens[0])]\n    ops = []\n    i = 1\n    while i < len(tokens):\n        op = tokens[i]\n        num = float(tokens[i+1])\n        if op in ("*", "/"):\n            prev = nums.pop()\n            nums.append(prev * num if op == "*" else prev / num)\n        else:\n            ops.append(op)\n            nums.append(num)\n        i += 2\n    result = nums[0]\n    for j, op in enumerate(ops):\n        result = result + nums[j+1] if op == "+" else result - nums[j+1]\n    return result\nprint(calc("2 + 3 * 4"))\nprint(calc("10 - 2 * 3 + 1"))', rk: ['def', 'for', 'if', 'return', 'while'], ml: 15 },
];

function buildPyChallenges() {
    return pyTopics.map((c, i) => ({
        id: i + 1,
        title: c.t,
        description: c.d,
        starterCode: c.s,
        hints: c.h,
        helper: c.hp,
        requiredKeywords: c.rk || [],
        bannedPatterns: c.bp || [],
        requiredPatterns: c.rp || [],
        expectedOutput: c.eo || null,
        preventHardcode: c.ph || false,
        minLines: c.ml || 1,
    }));
}

export const pythonChallenges = buildPyChallenges();
